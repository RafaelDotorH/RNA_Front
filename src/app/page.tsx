import Login from "./login/page"; //importa la página de login

export default function Home() { // Componente principal de la aplicación que renderiza la página de login
  return (
    <Login/>
  );
}
